# Evaluación de Prompt

**Ejercicio:** DIAGRAM_UML_FUNCTIONAL_EXTENSION
**Dominio:** PETCLINIC
**Fecha:** 9/3/2026, 21:57:18

## Índice
- [1. Recursos y Contexto](#1-recursos-y-contexto-oculto-al-usuario)
- [2. Prompt Enviado por el Usuario](#2-prompt-enviado-por-el-usuario-visible-y-editable)
- [3. Propuesta del Modelo](#3-propuesta-del-modelo)

---

## 1. RECURSOS Y CONTEXTO (Oculto al usuario)


---

## 2. PROMPT ENVIADO POR EL USUARIO (Visible y editable)
Una vez que tenemos la extensión funcional completa del nuevo examen, pasaremos a la siguiente tarea que quiero que realices.

Quiero que en base a la lógica de la extensión funcional que me has pasado, me generes un diagrama UML en código Mermaid similar al de los ejemplos que te he pasado en el documento md “functional_extension_examples”. Ten en cuenta estos requisitos:

-	Recuerda todo el contexto dado en la anterior petición.

-	De los enunciados de ejemplo, céntrate en los del proyecto del que hemos creado la extensión (ajedrez o clínica veterinaria)

     - De ellos, mantendrás la estructura (entidad, atributos, relaciones, direccionalidad, multiplicidad), de las clases base, es decir, las de color negro.

-	Para las nuevas clases a implementar por el alumno, es decir, clases rojas, añadirás toda su estructura (entidad, atributos, relaciones, direccionalidad, multiplicidad), acorde a la extensión funcional generada.

-	Para las relaciones, si estas tienen un nombre asignado, este debe constar en el diagrama.

-	El color del contenido de las clases negras debe ser negro también. 

-	El color del contenido de las clases rojas debe ser rojo también.

---

## 3. PROPUESTA DEL MODELO
classDiagram
    %% --- ENUMS (Tipos de datos para atributos, no definidas como clases en el diagrama) ---
    %% PieceColor: {WHITE, BLACK}
    %% PlayerType: {HUMAN, AI}
    %% GameStatus: {NOT_STARTED, IN_PROGRESS, CHECK, CHECKMATE, STALEMATE, DRAW, RESIGNED}
    %% MoveType: {NORMAL, CASTLING_KINGSIDE, CASTLING_QUEENSIDE, EN_PASSANT, PROMOTION}

    %% --------------------------------------------------------------------------------------
    %% CLASES NEGRAS: Lógica Central del Juego de Ajedrez (Sistema Base/Existente)
    %% --------------------------------------------------------------------------------------

    class Game {
        -Player currentPlayer
        -Board board
        -GameStatus status
        -List<Player> players
        +start()
        +makeMove(Square from, Square to)
        +isGameOver() bool
        +endGame()
    }
    style Game fill:#FFF,stroke:#333,color:#000

    class Player {
        -String name
        -PieceColor color
        -PlayerType type
        +makeMove(Game game)
        +getPieces() List<Piece>
    }
    style Player fill:#FFF,stroke:#333,color:#000

    class Board {
        -Square[8][8] squares
        +getSquare(int rank, int file) Square
        +setPieceAt(int rank, int file, Piece piece)
        +getPieceAt(int rank, int file) Piece
        +clone() Board
        +reset()
    }
    style Board fill:#FFF,stroke:#333,color:#000

    class Square {
        -int rank
        -int file
        -Piece piece
        +isEmpty() bool
        +setPiece(Piece p)
        +getPiece() Piece
        +clearPiece()
        +toString() String
    }
    style Square fill:#FFF,stroke:#333,color:#000

    class Piece {
        <<abstract>>
        -PieceColor color
        -Square currentSquare
        +isValidMove(Board board, Square start, Square end) bool
        +getPossibleMoves(Board board) List<Square>
        +move(Square newSquare)
        +getType() String
    }
    style Piece fill:#FFF,stroke:#333,color:#000

    class Pawn {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style Pawn fill:#FFF,stroke:#333,color:#000
    class Knight {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style Knight fill:#FFF,stroke:#333,color:#000
    class Bishop {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style Bishop fill:#FFF,stroke:#333,color:#000
    class Rook {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style Rook fill:#FFF,stroke:#333,color:#000
    class Queen {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style Queen fill:#FFF,stroke:#333,color:#000
    class King {
        +isValidMove(Board board, Square start, Square end) bool
    }
    style King fill:#FFF,stroke:#333,color:#000

    %% RELACIONES DE CLASES NEGRAS
    Piece <|-- Pawn
    Piece <|-- Knight
    Piece <|-- Bishop
    Piece <|-- Rook
    Piece <|-- Queen
    Piece <|-- King

    Game "1" -- "1" Board : gestiona
    Game "1" -- "2" Player : participan
    Board "1" -- "64" Square : contiene
    Square "0..1" -- "1" Piece : ocupa
    Piece "1" -- "1" Square : estaEn
    Player "1" -- "*" Piece : posee


    %% --------------------------------------------------------------------------------------
    %% CLASES ROJAS: Nueva Extensión Funcional (Historial de Movimientos)
    %% --------------------------------------------------------------------------------------

    class Move {
        -Square fromSquare
        -Square toSquare
        -Piece movedPiece
        -Piece capturedPiece (optional)
        -MoveType type
        +execute(Board board)
        +undo(Board board)
        +toString() String
    }
    style Move fill:#FFF,stroke:#333,color:#F00

    class GameHistory {
        -List<Move> moves
        -int currentMoveIndex
        +addMove(Move move)
        +undoLastMove() Move
        +redoMove() Move
        +reset()
        +canUndo() bool
        +canRedo() bool
        +getMoves() List<Move>
    }
    style GameHistory fill:#FFF,stroke:#333,color:#F00

    %% NUEVAS RELACIONES (entre clases negras y rojas, y entre clases rojas)
    Game "1" -- "1" GameHistory : registraHistorial
    GameHistory "1" -- "*" Move : contiene
    Move "1" -- "1" Square : origen
    Move "1" -- "1" Square : destino
    Move "1" -- "1" Piece : piezaMovida
    Move "1" -- "0..1" Piece : piezaCapturada